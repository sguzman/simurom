# Tranche 013 (2026-04-16) — add mode/cache/config-dir roadmap items

- Add core roadmap items for `dev`/`prod` mode, run-scoped file logging, and standardized cache/log directories
- Add core roadmap item to support default config directory `.config/simurom/`

