# Tranche 060 (2026-05-17) — Egui Timeline Keyboard Toggle

Source roadmaps: `docs/roadmaps/ui-roadmap.md`

- [x] UI (M2): Hide the Egui Timeline overlay by default in gameplay (`simurom`) scenes, while keeping it visible by default in viewer (`simurom-viewer`) scenes.
- [x] UI (M2): Introduce a keyboard toggle system (responsive to `Tab` and `Backquote` `~` keys) to show/hide the Egui panels during runtime in both applications.
