# Tranche: Export duration defaults + baked shader packaging (2026-04-27)

Roadmap items in this tranche (only):

- [x] `docs/roadmaps/export-roadmap.md` — Default export duration/fps to the bake/scene values when not overridden (avoid accidental 0.1s exports from control-pane defaults)
- [x] `docs/roadmaps/export-roadmap.md` — Package builtin engine shaders (`assets/simurom/shaders/*`) into bake artifacts for self-contained playback/export

